source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby "3.1.0"

group :development do
  gem 'rake'

  # Intellisense and autocomplete for VSCode
  gem 'solargraph'
end
